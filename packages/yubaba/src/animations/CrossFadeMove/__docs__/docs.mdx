---
name: CrossFadeMove
route: /cross-fade-move
---

import { Playground, PropsTable } from 'docz';
import Select from 'react-select';
import { Toggler } from 'yubaba-common';
import Baba from '../../../Baba';
import CrossFadeMove from '../index';

# CrossFadeMove

Is a composite animation of [CrossFade](/cross-fade) and [Move](/move).
Useful for animating an element moving from one position to another.
Works best when the element has **stark visual differences**, but is still of a similar aspect ratio.

## Usage

<Playground>// TODO</Playground>

## Props

<PropsTable of={CrossFadeMove} />
